# Combined requirements for the integrated Viva backend.
# Equivalent to: pip install -r part_a/requirements.txt -r part_b/requirements.txt -r part_c/requirements.txt

# --- Part B: Data & Scoring ---
pytest>=7.0

# --- Part C: Agent Orchestration (FastAPI app) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
openai>=1.30
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27  # used by tests/test_integration.py's FastAPI TestClient

# --- Part A: Voice & Ingestion ---
# Heavy ML deps — the app degrades gracefully (stub transcribe/RAG) if
# these aren't installed, so they're listed separately rather than
# forcing every contributor to install them just to run the API/tests.
# Install with: pip install -r part_a/requirements.txt
#   faster-whisper==1.0.3
#   sentence-transformers==2.7.0
#   pdfplumber==0.11.4
#   python-docx==1.1.2
#   numpy
# Also requires ffmpeg on PATH (not a pip package) for transcribe().
